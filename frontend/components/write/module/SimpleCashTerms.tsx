import { Text } from "@chakra-ui/react";

export function SimpleCashTerms() {
  return (
    <Text fontSize="lg" fontWeight={600} p={0}>
      {"Funds are locked until cashed by the recipient."}
    </Text>
  );
}
