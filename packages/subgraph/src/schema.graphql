type Cheque @entity {
  id: ID!
  sender: Sender!
  createdAt: BigInt!
  transactionHash: String!
}

type Sender @entity {
  id: ID!
  address: Bytes!
  cheques: [Cheque!] @derivedFrom(field: "sender")
  createdAt: BigInt!
  chequeCount: BigInt!
}
