{
  "name": "@cheq/subgraph",
  "license": "UNLICENSED",
  "version": "1.0.0",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ --ipfs https://api.thegraph.com/ipfs/ GITHUB_USERNAME/cheq",
    "create-local": "graph create --node http://localhost:8020/ Cheq/Cheq",
    "remove-local": "graph remove --node http://localhost:8020/ Cheq/Cheq",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 Cheq/Cheq"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.22.1",
    "@graphprotocol/graph-ts": "^0.22.1"
  },
  "devDependencies": {
    "mustache": "^3.1.0"
  }
}
