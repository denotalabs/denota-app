specVersion: 0.0.2
schema:
  file: schema.graphql
dataSources:
  - kind: ethereum/contract
    name: Events
    network: mumbai
    source:
      address: "0x548C4f87180051eC5D2630ac72002d0172FdDf62"
      abi: Events
      startBlock: 32267354
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.5
      language: wasm/assemblyscript
      entities:
        - Cheq  # Cheqs
        - Transfer  # Write/Transfers
        - Escrow  # Fund/Cash
        - ERC20  # ERC20s that have interacted with the protocol
        - Account  # Accounts that have interacted with CheqRegistrar
        - DirectSend  # Payment modules
        - DirectPayModule
        - DirectPayData
        # - DirectPayData
      abis:
        - name: Events
          file: ../../out/Events.sol/Events.json
      eventHandlers:
        # - event: ModuleBaseConstructed(indexed address,uint256)
        # - event: ModuleWhitelisted(indexed address,indexed address,bool,bool,uint256)
        # - event: RuleWhitelisted(address,indexed address,indexed bool,uint256)
        # - event: TokenWhitelisted(address,indexed address,indexed bool,uint256)
        - event: Written(indexed uint256,indexed address,(indexed address,uint256,uint256,address,address,address,uint256),uint256,bytes,uint256,uint256,uint256)
          handler: handleWrite
        - event: Transferred(indexed uint256,indexed address,indexed address,uint256,uint256,uint256)
          handler: handleTransfer
        - event: Funded(indexed address,indexed uint256,uint256,uint256,indexed bytes,uint256,uint256,uint256)
          handler: handleFund
        - event: Cashed(indexed address,indexed uint256,address,uint256,indexed bytes,uint256,uint256,uint256)
          handler: handleCash
      file: mapping.ts
  # - kind: ethereum/contract
  #   name: CheqRegistrar
  #   network: {{network}}
  #   source:
  #     address: "{{registrar}}"
  #     abi: CheqRegistrar
  #     startBlock: 31884801
  #   mapping:
  #     kind: ethereum/events
  #     apiVersion: 0.0.5
  #     language: wasm/assemblyscript
  #     entities:
  #       - Cheq  # Cheqs
  #       - Transfer  # Write/Transfers
  #       - Escrow  # Fund/Cash
  #       - ERC20  # ERC20s that have interacted with the protocol
  #       - Account  # Accounts that have interacted with CheqRegistrar
  #       - DirectSend  # Payment modules
  #       - DirectPayModule
  #       - DirectPayData
  #       # - DirectPayData
  #     abis:
  #       - name: CheqRegistrar
  #         file: ../../out/CheqRegistrar.sol/CheqRegistrar.json
  #     eventHandlers:
  #       # - event: Written(uint256,address,struct DataTypes.Cheq memory,uint256,bytes memory,uint256,uint256,uint256)
  #       #   handler: handleWrite
  #       # - event: Transferred(uint256 indexed,address indexed,address indexed,uint256,uint256,uint256)
  #       #   handler: handleTransfer
  #       # - event: Funded(address indexed,uint256 indexed,uint256,uint256,bytes indexed,uint256,uint256,uint256)
  #       #   handler: handleFund
  #       # - event: Cashed(address indexed,address,uint256 indexed,bytes indexed,uint256,uint256,uint256)
  #       #   handler: handleCash
  #     file: mapping.ts